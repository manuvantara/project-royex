import { Button } from '@/components/ui/button';

export default function AcceptButton({ selectedRowIds }: { selectedRowIds: string[] }) {
  // TODO: Add logic to accept offers

  return (
    <Button variant="default" onClick={() => {}} type="button">
      Accept offer(s)
    </Button>
  );
}
